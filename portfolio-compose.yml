version: '3'
services:
  website:
    image: nginx:alpine
    container_name: portfolio-site
    ports:
      - "8081:80"
    volumes:
      - /home/portfolio/www:/usr/share/nginx/html:ro
    restart: always
